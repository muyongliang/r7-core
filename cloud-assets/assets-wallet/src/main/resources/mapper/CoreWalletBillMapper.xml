<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.r7.core.assets.wallet.mapper.CoreWalletBillMapper">

    <select id="pageWalletBill" resultType="com.r7.core.assets.wallet.vo.CoreWalletBillPageVO">
        select id, app_id, user_id, organ_id, transaction_user_id,
        transaction_amount, type, source, status, description, created_at
        from core_wallet_bill
        <where>
            user_id=#{userId}
        </where>
        order by created_at
    </select>

</mapper>